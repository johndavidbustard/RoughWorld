package ai.inference;

public class InferState 
{
	//Take a sensed estimate and produce a more complete InferedState
	
	//One approach is to find partial matches between the sensed objects (and their states) and any object models
	//
}
